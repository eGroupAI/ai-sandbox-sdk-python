[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-sandbox-sdk-python"
version = "1.0.0"
description = "Official Python SDK for AI Sandbox Integration API"
requires-python = ">=3.10"
dependencies = ["requests>=2.32.0"]
